{
    server: {
        port: ${EMAIL_SERVER_PORT}
    },
    smtp: {
        host: ${EMAIL_SMTP_HOST},
        port: ${EMAIL_SMTP_POST},
        username: ${EMAIL_SMTP_USERNAME},
        password: ${EMAIL_SMTP_PASSWORD},
    },
    security: {
        username:  ${EMAIL_SECURITY_USERNAME},
        password:  ${EMAIL_SECURITY_PASSWORD}
    }
}